type Query = {
    limit: number | null;
    skip: number | null;
    tag: string[] | null;
    cookTimeLess: number | null;
}

async function get(query: Query): Promise<any> {
    

    try {
        fetch()
    }
    catch (e) {
        
    }
}

export default {
    
}
